spring:
  jpa:
    hibernate:
      ddl-auto: create-drop  #todo 1.2 none, чтобы исользовать рукописную схему в тестах, затем 1.3
    properties:
      show_sql: true
      format_sql: true
      type: trace
  datasource:
    url: 'jdbc:mysql://localhost:3306/jpa_hibernate'
    username: 'bibis'
    password: 'bibis'
    name: 'source_name'
    initialization-mode: always  # todo 1.3 never чтобы использовать рукописную вхему в тестах. Конец
server:
  port: 8080